'use strict'; // To reduce error so use this world('use strict') in the first line;

let user = 'Sakthi';

// use = 'Arun'; // wrong key world, but you not use ('use strict') this they will answer it;

console.log(user);

// const interface = 'Car'; // reserved key world(interface);
// const private = 'Jack'; // reserved key world(private);
